@model List<Libro>

<div class="container mt-4">
    <h2 class="mb-4">Elenco Libri</h2>

    @if (Model.Count == 0)
    {
        <div class="alert alert-info">Nessun libro disponibile.</div>
    }
    else
    {
        <table class="table table-striped table-bordered table-hover">
            <thead class="table-dark">
                <tr>
                    <th>Id</th>
                    <th>Titolo</th>
                    <th>Autore</th>
                    <th>Anno</th>
                    <th>Paese</th>
                    <th>Lingua</th>
                    <th>Prezzo (€)</th>
                    <th>Pagine</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var libro in Model)
                {
                    <tr>
                        <td>@libro.IdLibro.ToString()</td>
                        <td>@libro.Titolo</td>
                        <td>@libro.Autore.Cognome @libro.Autore.Nome</td>
                        <td>@libro.Anno.ToString()</td>
                        <td>@libro.Paese.Nome</td>
                        <td>@libro.Lingua.Nome</td>
                        <td>@libro.Prezzo.ToString("F2")</td>
                        <td>@libro.Pagine.ToString()</td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>
